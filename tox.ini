[tox]
envlist = black, check-manifest

[testenv:black]
skip_install = true
deps = black
commands = black --check --diff {toxinidir}

[testenv:check-manifest]
skip_install = true
deps = check-manifest
commands = check-manifest
